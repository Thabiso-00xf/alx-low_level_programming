#include "main.h"
#include <stdio.h>

/**
 *_strlen - the string´s lenght
 *
 *@s: input char parameter
 *Return: lenght of a string
 */

int _strlen(char *s)

{
	int j;

	for (j = 0; s[j] != '\0'; j++)
	{
	}
		return (j);
}
