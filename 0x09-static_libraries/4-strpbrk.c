#include "main.h"
#include <stdio.h>
/**
 * _strpbrk - bytes
 * @s:is a pointer to char
 * @parameter pointer to char
 * Return: NULL
 */
char *_strpbrk(char *s, char *accept)
{
	(void)s;
	(void)accept;
	return (0);
}
